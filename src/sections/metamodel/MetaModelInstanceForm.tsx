import { InstanceMetaField, MetaField, MetaModel } from "src/frontend-utils/types/metamodel";

export default function MetaModelInstanceForm({
  metaModel,
}: {
  metaModel: MetaModel;
}) {

  return (<div></div>)
}
